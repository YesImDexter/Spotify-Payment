/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{html,js}', // Include all HTML and JS files in src
    './public/index.html',  // Include the HTML file in public],
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}